spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/apifinance
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

jwt:
   public:
     key: classpath:key.pub
   private:
    key: classpath:app.key